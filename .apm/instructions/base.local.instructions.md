---
description: "Basic instructions for the project"
applyTo: "*"
---

### General

- This project uses `generic methods`, which can be enabled by `export GOEXPERIMENT=genericmethods`
- Use `gotip` instead of `go` command because the project is `gotip`
